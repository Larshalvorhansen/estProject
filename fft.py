import numpy as np
import matplotlib.pyplot as plt
from scipy import fft, ifft


def functionGenerator(SNR):
    A = 
    x = A * np.exp(1j*(w_0*t + phi)) + w_t
    return(x,A)

x = np.array([1.0, 2.0, 1.0, -1.0, 1.5])

def FFT(x):
    return fft(x)

print(FFT(x))